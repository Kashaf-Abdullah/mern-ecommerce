// Jest setup file
// Add any global test configurations here

// Increase timeout for database operations
jest.setTimeout(30000);

// Mock console.log during tests (optional)
// global.console.log = jest.fn();
